Then(/^user is on cart_page$/) do
  if ST_AllPage.isStage?
    ST_ProductPage.get_cart_info
  elsif REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user clear items in cart list$/) do
if REV_AllPage.isRevolve?
p  "REV step definition in progress"
p "REV step definition in progress"
  end
end

And(/^user can see expected item in cart$/) do
if REV_AllPage.isRevolve?
  p  "REV step definition in progress"
  p "REV step definition in progress"
  end
end

And(/^user clicks on afterpay info$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user can see afterpay info modal$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user close afterpay info modal$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user can see sign in message in the cart bottom$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user clicks on sign in link in sign in message$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user clicks on shipping options in cart$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user clicks on checkout as guest button on cart page$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user click "([^"]*)" button on cart page$/) do |arg|
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user can see empty shopping cart banner$/) do
if REV_AllPage.isRevolve?
  p  "REV step definition in progress"
  p "REV step definition in progress"
end
end

And(/^user can see start shopping message on cart page$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user can see shop "([^"]*)" on cart page$/) do |arg|
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user scrolls to the bottom of cart page$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user is on top of cart page$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user is on paypal checkout page$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user click on paypal button on cart page$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^estimated total price is correct$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user can see total price on top and bottom of cart page$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user click on remove for random product$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user can see removed from bag message$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user click on save for later for random product$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user can see saved for later message$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user can see expected item in saved for later block$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user clicks on sign in for faster checkout button$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user click on move to bag for saved item$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user can see moved to bag message$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^There is no saved for later block$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user click remove for saved item$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user can see removed from saved message$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^There is no expected item in cart$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user can see product qty in cart$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user chose random qty for random item in cart$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user can see size updated message$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user can see that qty was changed for expected item in cart$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user can see that size was changed for expected item in cart$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user can see product size in cart$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user chose random size for random item in cart$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user can see product "([^"]*)" for item in cart$/) do |arg|
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end

And(/^user click "([^"]*)" on cart_page$/) do |element_name_to_click|
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  elsif
  VN_AllPage.isVenus?
    VN_CartPage.click_cart_page_content(element_name_to_click)
  end
end

And(/^user can see subtotal count on top and bottom of cart page$/) do
  if REV_AllPage.isRevolve?
    p  "REV step definition in progress"
    p "REV step definition in progress"
  end
end


And(/^guest user set offer_code field	with "([^"]*)" on cart_page$/) do |arg|
  if VN_AllPage.isVenus?
    p  "VEN step definition in progress"
    p "VEN step definition in progress"
  end
end

And(/^guest user can see "([^"]*)" on cart_page$/) do |arg|
  if VN_AllPage.isVenus?
    p  "VEN step definition in progress"
    p "VEN step definition in progress"
  end
end

And(/^user can see "([^"]*)" with value "([^"]*)" on cart_page$/) do |element_name, element_value|
  if VN_AllPage.isVenus?
    # VN_CartPage.check_element_with_value_on_cart_page(element_name, element_value)
    VN_CartPage.check_item_locator_value(element_name, element_value)
  end
end

And(/^user can see "([^"]*)" on cart_page$/) do |content_name_to_check|
  if VN_AllPage.isVenus?
   VN_CartPage.expect_cart_page_content(content_name_to_check)
  end
end

And(/^user can see "([^"]*)" for "([^"]*)" on cart_page$/) do |arg1, arg2|
  if VN_AllPage.isVenus?
    p  "VN step definition in progress"
    p "VN step definition in progress"
  end
end


And(/^user set "([^"]*)" with value "([^"]*)" on cart_page$/) do |element_name, element_value|
  if VN_AllPage.isVenus?
    VN_CartPage.set_item_qty_with_value(element_name, element_value)
  end
end

And(/^user clear cart_page$/) do
  if VN_AllPage.isVenus?
    VN_CartPage.clear_cart_page
  end
end

And(/^user can see order_total_value on cart_page$/) do
  if VN_AllPage.isVenus?
    VN_CartPage.check_order_total
  end
end

And(/^user fill offer_code_field with offer_code on cart_page$/) do
  if VN_AllPage.isVenus?
    VN_CartPage.fill_offer_code_field_with_offer_code
  end
end


And(/^user can see applied_offer_code on cart_page$/) do
  if VN_AllPage.isVenus?
    VN_CartPage.check_applied_offer_code
  end
end